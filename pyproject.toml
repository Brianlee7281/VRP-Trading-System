[project]
name = "vrp-trading"
version = "0.1.0"
description = "Regime-conditioned systematic short volatility on SPX options"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py311"
